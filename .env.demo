# Demo Environment Variables for Testing
# Copy this to .env for local testing (DO NOT use these values in production)

# ===== DEMO/TEST CONFIGURATION =====
# These are example/test values for local development

# Authentication Secrets (demo values - generate new ones for production)
JWT_SECRET=demo-jwt-secret-key-for-testing-only-minimum-64-characters-needed
SESSION_SECRET=demo-session-secret-key-for-testing-only-change-in-production

# Frontend URL (for local development)
FRONTEND_URL=http://localhost:5173

# Stripe Configuration (test/demo values - replace with real ones)
STRIPE_SECRET_KEY=sk_test_demo_key_replace_with_real_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_demo_key_replace_with_real_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_demo_webhook_secret_replace_with_real_webhook_secret

# Stripe Price IDs (demo values - replace with real price IDs from Stripe)
STRIPE_STARTER_PRICE_ID=price_demo_starter_monthly_replace_with_real_id
STRIPE_PRO_PRICE_ID=price_demo_pro_monthly_replace_with_real_id
STRIPE_ENTERPRISE_PRICE_ID=price_demo_enterprise_monthly_replace_with_real_id

# Stripe URLs (for local testing)
STRIPE_SUCCESS_URL=http://localhost:5173/success
STRIPE_CANCEL_URL=http://localhost:5173/cancel
STRIPE_PORTAL_RETURN_URL=http://localhost:5173/account

# OpenAI API Key (replace with your real key)
OPENAI_API_KEY=sk-demo-openai-key-replace-with-real-openai-api-key

# Optional Services (uncomment and set if needed)
# SENDGRID_API_KEY=SG.demo-sendgrid-key-replace-with-real-key
# FROM_EMAIL=noreply@yourdomain.com
# ADMIN_KEY=demo-admin-key-for-testing

# Development Settings
NODE_ENV=development
LOG_LEVEL=info
PORT=8080

# Usage Limits
FREE_MESSAGE_LIMIT=20
FREE_MONTHLY_LIMIT=150